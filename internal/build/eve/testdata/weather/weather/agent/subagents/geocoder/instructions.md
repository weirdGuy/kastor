Resolves place names to coordinates

## Inputs

Inputs arrive in the user message.

- `location` (string, required)

## Outputs

Reply with a single JSON object holding exactly these fields:

- `coordinates` (string)
